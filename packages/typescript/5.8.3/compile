#!/usr/bin/env bash

# Put instructions to compile source code, remove this file if the language does not require this stage

rename 's/$/\.ts/' "$@" # Add .ts extension

#tsc *.ts --target ES2020 --module commonjs --types node

# tsc -p tsconfig.json *.ts
tsc -p tsconfig.json
